[
    {
        "key": "ClientId",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "ClientId",
            "description": "The ClientId to connect to the API",
            "required": true
        }
    },
    {
        "key": "ClientSecret",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "type": "password",
            "label": "ClientSecret",
            "required": true
        }
    },
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "https://proxies-<environment>.rovictonline.nl/idp-proxy",
        "templateOptions": {
            "label": "BaseUrl",
            "description": "The URL to the API.",
            "required": true
        }
    },
    {
        "key": "BaseUrlToken",
        "type": "input",
        "defaultValue": "https://sts-dev.rovictonline.nl/connect/token",
        "templateOptions": {
            "label": "BaseUrlToken",
            "description": "The URL to get the bearer token.",
            "required": true
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
          "label": "Toggle debug logging",
          "description": "When toggled, debug logging will be displayed",
          "required": false
        }
    },
    {
        "key": "XVendorCode",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "XVendorCode",
            "description": "The XVendorCode to connect to the API",
            "required": true
        }
    },
    {
        "key": "XVerificatieCode",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "XVerificatieCode",
            "description": "The XVerificatieCode to connect to the API",
            "required": true
        }
    },
    {
        "key": "CompanyNumber",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "CompanyNumber",
            "description": "The CompanyNumber (BestuursNummer) to connect to the API",
            "required": true
        }
    },
    {
        "key": "Department",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "Department",
            "description": "The Department (Vestiging) to connect to the API",
            "required": true
        }
    }
]
